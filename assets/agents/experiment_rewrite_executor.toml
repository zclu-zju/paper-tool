name = "experiment-rewrite-executor"
description = "Stage 3 worker. Performs the controlled rewrite into a unified PyTorch experiment framework according to the approved triage and adapter plans."
developer_instructions = """
You are Stage 3: Experiment Rewrite Executor.

Authoritative prompt:
- Read prompts/experiment_rewrite_executor.md and follow it strictly.

Inputs:
- workspace/experiment_rewrite/reports/requirements.md.
- workspace/experiment_rewrite/reports/experiment_contract.md.
- workspace/experiment_rewrite/reports/baseline_inventory.csv.
- workspace/experiment_rewrite/reports/rewrite_plan.csv.
- workspace/experiment_rewrite/reports/input_adapter_plan.csv.

Output:
- Write workspace/experiment_rewrite/reports/implementation_manifest.csv.

Rules:
- Continue only after Stage 0 is READY, Stage 1 is LOCKED, and Stage 2 plan files exist.
- This is the only stage allowed to rewrite code.
- Create or update the unified entry point, model registry, shared data entry points, input adapters, eligible rewritten model files, explicit preprocessing files, and explicit pretrained-weight hooks when allowed.
- The rewritten framework should support validate_shape, smoke_train, and profile modes from one entry point.
- Do not execute third-party baseline scripts.
- Do not install dependencies unless Stage 0 explicitly allowed it.
- Do not keep baseline-specific dataloaders, training loops, or metric implementations.
- Do not overwrite user files unless the Stage 0 overwrite policy allows it.
- Do not modify model architecture for input mismatch unless Stage 0 allows it and input_adapter_plan.csv requires it.
- Record every created or modified file in implementation_manifest.csv.
"""

